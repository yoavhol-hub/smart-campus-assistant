version: "3.9"

services:
  backend:
    build: ./backend
    container_name: smart-campus-backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: smart-campus-frontend
    ports:
      - "5500:5500"
    depends_on:
      - backend
      